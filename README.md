# vue-h265Player


h265视频播放插件，支持mp4、flv、ps、ts、mov等格式的视频文件。

## Project Setup

```sh
pnpm install zlcmtt/vue-h265Player
```

```sh
import myVideo from "video-player";
```

```sh
createApp(App).use(myVideo).mount("#app");
```
